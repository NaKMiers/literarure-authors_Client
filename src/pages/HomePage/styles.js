import { makeStyles } from '@mui/styles'

export default makeStyles(theme => ({
   homePage: {
      maxWidth: 1024,
      margin: 'auto',
      minHeight: '100vh',
      padding: '0px 12px',
   },

   main: {
      marginTop: 128,
   },
}))
